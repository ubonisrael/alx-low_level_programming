#include <stdio.h>

/**
 * main - prints the name of the program
 * @argv: array of arguments passed to the program
 * Return: 0
 */

int main(char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
