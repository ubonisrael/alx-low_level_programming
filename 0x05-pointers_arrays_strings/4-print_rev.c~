#include "main.h"

/**
 * print_rev - prints a string in reverse
 * @s: string
 * Return: void
 */

void print_rev(char *s)
{
	int length;
	int x;

	length = 0;
	while (*s++ != '\0')
		length++;
	for (x = length - 1; x >= 0; x--)
		_putchar(*(s + x));
	_putchar('\n');
}
